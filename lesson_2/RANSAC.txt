```
1. 假定取P个数据点{x1， x2, x3, ..., xP}拟合一个至少由n个点决定的模型
2. 设迭代书k = 1
3. 从P中随机选取n个点拟合一个模型，记为M1。 n的初始值为3，并逐步增大
4. 给定允许最大误差Σ，计算数据点{x1, x2, ..., xn}中相对于模型的残差在偏差Σ内的元素个数，如果内点个数大于阈值t，
则算法终止。之后我们可以根据内点集合重新拟合模型（可以利用最小二乘法或其变种）。
5. 设k = k + 1，如果k小于预先设定的k，跳至第3步，新的内点集合和模型分别记为S1*和M1*
```